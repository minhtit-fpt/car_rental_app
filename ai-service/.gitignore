__pycache__/
*.pyc
.venv/
venv/
.env
# Index cache sinh ra khi build (embedding bge-m3) — không commit.
data/index.pkl
data/index/
*.index
# Snapshot admin (backend xuất) — chứa PII người dùng (SĐT/email), KHÔNG commit.
data/admin_snapshot.json
