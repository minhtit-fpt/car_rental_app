# RAG chatbot service — chạy LLM/embedding/VLM local qua LM Studio
# (OpenAI-compatible API tại localhost:1234/v1).

fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27           # gọi LM Studio OpenAI-compatible endpoints
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# Test
pytest>=8.3

# Rerank (tùy chọn, B-sau) — heuristic trước, nâng cấp bge-reranker khi cần:
# FlagEmbedding>=1.3
